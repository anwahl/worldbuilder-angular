import { MatchEmailDirective } from './match-email.directive';

describe('MatchEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchEmailDirective();
    expect(directive).toBeTruthy();
  });
});
